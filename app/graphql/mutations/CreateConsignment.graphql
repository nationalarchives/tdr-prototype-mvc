mutation createCon ($name: String!, $seriesId: Int!){
    createConsignment(name:$name, seriesId:$seriesId) {
        name,
        id,
        series {
            id,
            name,
            description
        }
    }
}