query getUser($providerKey: String!, $providerId: String!) {
    getUser(providerKey: $providerKey, providerId: $providerId) {
        id
        firstName
        lastName
        email
        providerId
        providerKey
    }
}