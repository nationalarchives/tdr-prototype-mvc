{
  "name": "prototype-cypress",
  "version": "1.0.0",
  "main": "index.ts",
  "author": "Sam Palmer <github@sampalmer.dev>",
  "license": "MIT",
  "devDependencies": {
    "@cypress/webpack-preprocessor": "^4.1.0",
    "cypress": "^3.4.1",
    "ts-loader": "^6.0.4",
    "typescript": "^3.5.3",
    "webpack": "^4.38.0",
    "wait-on": "^3.3.0"
  },
  "scripts": {
    "ci": "cypress run --record --key $CYPRESS_KEY",
    "wait": "wait-on http://localhost:9000",
    "cypress:ci": "cypress run --record --key $CYPRESS_KEY"
  }
}
